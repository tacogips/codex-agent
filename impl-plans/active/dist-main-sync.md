# Distribution Artifact Synchronization Implementation Plan

**Status**: Completed
**Design Reference**: `design-docs/specs/architecture.md#distribution-artifact-synchronization`
**Created**: 2026-05-28
**Last Updated**: 2026-05-28

## Source Context

This plan implements the accepted issue-resolution design for the delegated
recent-change review finding from `codex-recent-change-quality-loop` /
`step3-handoff`.

- **Parent Workflow Execution**:
  `div-codex-recent-change-quality-loop-1779949794-40badc08`
- **Current Workflow Execution**:
  `div-codex-design-and-implement-review-loop-1779949982-519185f1`
- **Reviewed Range**: `f92afce1a1b691d45bb3fa05c476e744efa84d8f..HEAD`
- **Finding**: `dist/main.js:1` is out of sync with current TypeScript source
  output; `bun run check:dist-sync` rebuilt it and exited `1`.
- **Review Decision**: Blocking, mid severity.
- **Scope**: Synchronize the generated distribution artifact. Do not manually
  edit bundle logic. Do not change source files unless generated diff inspection
  identifies build-tool nondeterminism that requires a small tooling correction.

## Codex-Agent References

| Path | Purpose |
| --- | --- |
| `dist/main.js` | Primary deliverable; generated published Bun runtime artifact. |
| `src/process/manager.ts` | Source behavior reference for recent Codex exec option parsing changes. |
| `src/process/manager.test.ts` | Targeted process-manager regression verification. |
| `src/process/types.ts` | Source type reference for process/session option shape. |
| `src/sdk/session-runner.ts` | Source behavior reference for session execution path changes. |
| `package.json` | Script and export reference for `build`, `check:dist-sync`, `lint:biome`, `typecheck`, and `test`. |

## Tasks

### TASK-001: Generate Distribution Artifact

**Status**: Completed
**Parallelizable**: No
**Deliverable**: `dist/main.js`
**Dependencies**: None

Run the accepted production generation path:

```bash
bun run build
```

Keep source files unchanged unless the generated output reveals a determinism
problem that cannot be resolved by regenerating `dist/main.js`.

**Completion Criteria**:

- [x] `bun run build` completes successfully.
- [x] `dist/main.js` is regenerated by the build command.
- [x] No manual bundle logic edits are made.
- [x] No source files are edited during this task.

### TASK-002: Inspect Generated Diff

**Status**: Completed
**Parallelizable**: No
**Deliverable**: Diff inspection notes in this plan progress log
**Dependencies**: TASK-001

Inspect the generated artifact diff:

```bash
git diff -- dist/main.js
```

Confirm the diff reflects expected bundler output for the current source
behavior, especially recent changes around Codex exec option parsing and system
prompt support. If the diff is unexpectedly large or unrelated, check for
build-tool nondeterminism before expanding scope beyond `dist/main.js`.

**Completion Criteria**:

- [x] Diff inspection is recorded in the progress log.
- [x] Diff is classified as expected generated artifact output or a
  determinism concern.
- [x] Any scope expansion is justified in the progress log before source or
  tooling edits are made.

### TASK-003: Verify Distribution Sync Gate

**Status**: Completed
**Parallelizable**: No
**Deliverable**: Verification command result
**Dependencies**: TASK-001, TASK-002

Run the release gate that failed in the upstream review:

```bash
bun run check:dist-sync
```

This must pass without leaving `dist/main.js` modified.

**Completion Criteria**:

- [x] `bun run check:dist-sync` passes.
- [x] `git status --short -- dist/main.js` remains stable after the sync check.
- [x] Any failure is investigated before downstream review.

### TASK-004: Run Targeted Quality Checks

**Status**: Completed
**Parallelizable**: No
**Deliverables**: Verification command results
**Dependencies**: TASK-003

Run the accepted verification commands:

```bash
bun run lint:biome
bun run typecheck
bun run test -- src/process/manager.test.ts src/sdk/session-runner.test.ts src/sdk/agent-runner.dist-runtime.test.ts
```

Then consider full test coverage because `dist/main.js` is a published runtime
artifact:

```bash
bun run test
```

**Completion Criteria**:

- [x] Biome lint passes.
- [x] Type checking passes.
- [x] Targeted tests pass.
- [x] Full `bun run test` is run or explicitly deferred with rationale in the
  progress log.

## Dependencies

| Task | Depends On | Reason |
| --- | --- | --- |
| TASK-001 | None | Artifact must be generated before inspection. |
| TASK-002 | TASK-001 | Diff inspection requires regenerated output. |
| TASK-003 | TASK-001, TASK-002 | Sync gate should run after expected diff is confirmed. |
| TASK-004 | TASK-003 | Quality checks should verify the final synchronized state. |

## Parallelization

No tasks are parallelizable. Each task depends on the artifact state produced or
validated by the previous task, and concurrent writes to `dist/main.js` would
make diff inspection unreliable.

## Completion Criteria

- [x] `dist/main.js` is synchronized using `bun run build`.
- [x] `git diff -- dist/main.js` is inspected and summarized.
- [x] No manual generated-bundle logic edits are made.
- [x] Scope remains limited to `dist/main.js` unless build nondeterminism is
  documented.
- [x] `bun run check:dist-sync` passes.
- [x] `bun run lint:biome` passes.
- [x] `bun run typecheck` passes.
- [x] Targeted tests pass:
  `bun run test -- src/process/manager.test.ts src/sdk/session-runner.test.ts src/sdk/agent-runner.dist-runtime.test.ts`.
- [x] Full `bun run test` is run or explicitly deferred.
- [x] Workflow issue reference remains traceable to
  `codex-recent-change-quality-loop` / `step3-handoff`.

## Progress Log Expectations

Each implementation session must append a dated entry with:

- tasks completed or skipped;
- files changed;
- generated diff summary for `dist/main.js`;
- verification commands and outcomes;
- whether build nondeterminism was suspected;
- residual risks or explicit none.

## Progress Log

### Session: 2026-05-28 15:40 JST

**Tasks Completed**: TASK-001, TASK-002, TASK-003, TASK-004.

**Files Changed**: `dist/main.js`, `impl-plans/active/dist-main-sync.md`.

**Generated Diff Summary**: `bun run build` regenerated only `dist/main.js`.
The diff is confined to generated bundle output (`7030` insertions, `7036`
deletions) and is primarily GraphQL module ordering/chunk placement churn from
Bun's bundle output. No TypeScript source files were edited and no manual bundle
logic edits were made.

**Verification Commands and Outcomes**:

- `bun run build` - passed.
- `git diff -- dist/main.js` - inspected; generated artifact diff only.
- `bun run check:dist-sync` - passed and left `dist/main.js` stable.
- `bun run lint:biome` - passed.
- `bun run typecheck` - passed.
- `bun run test -- src/process/manager.test.ts src/sdk/session-runner.test.ts src/sdk/agent-runner.dist-runtime.test.ts` - passed, 29 tests.
- `bun run test` - passed, 242 tests.

**Build Nondeterminism Suspected**: Superseded by the 2026-05-28 15:46 JST
rerun below. Step 7 later observed a stale intermediate artifact and hash
variation, so the rerun explicitly verified repeated stable build hashes.

**Residual Risks**: None identified for the scoped `dist/main.js`
synchronization issue.

### Session: 2026-05-28 15:46 JST

**Tasks Completed**: Step 7 revision feedback addressed for TASK-001, TASK-002,
TASK-003, and TASK-004.

**Files Changed**: `dist/main.js`, `impl-plans/active/dist-main-sync.md`.

**Generated Diff Summary**: Step 7 reported that `bun run check:dist-sync`
failed after observing consecutive build hashes
`21982644506bfaaa4446458e5d85040c5bcb581de29a35e5827bccb0672bef4c` and
`9387522a5906ce2890865007f0fff81a2b82e2b51be11b12eeab0a16550964a8`. The
revision reran `bun run build` from the post-review artifact state, which moved
`dist/main.js` to the stable generated hash
`21982644506bfaaa4446458e5d85040c5bcb581de29a35e5827bccb0672bef4c`. Five
additional consecutive `bun run build` executions all produced the same hash and
reported `183` bundled modules.

**Verification Commands and Outcomes**:

- `bun run build` - passed and regenerated `dist/main.js` to the stable hash.
- `shasum -a 256 dist/main.js; bun run build; shasum -a 256 dist/main.js; bun run build; shasum -a 256 dist/main.js` - passed; hash stayed `21982644506bfaaa4446458e5d85040c5bcb581de29a35e5827bccb0672bef4c`.
- `for i in 1 2 3 4 5; do bun run build >/tmp/codex-build-$i.log 2>&1 || exit $?; printf "$i "; shasum -a 256 dist/main.js; rg "Bundled" /tmp/codex-build-$i.log; done` - passed; all five builds stayed at hash `21982644506bfaaa4446458e5d85040c5bcb581de29a35e5827bccb0672bef4c` and `183` modules.
- `bun run check:dist-sync` - passed and left `dist/main.js` stable.
- `bun run lint:biome` - passed.
- `bun run typecheck` - passed.
- `bun run test -- src/process/manager.test.ts src/sdk/session-runner.test.ts src/sdk/agent-runner.dist-runtime.test.ts` - passed, 29 tests.
- `bun run test` - passed, 242 tests.

**Build Nondeterminism Suspected**: The Step 7 artifact state showed a transient
stale/generated mismatch. After regenerating to the stable hash, repeated builds
and the sync gate were deterministic.

**Residual Risks**: Low residual risk remains that Bun may have produced one
transient alternate ordering from the previous artifact state, but the final
artifact now passes `check:dist-sync` and repeated hash-stability verification.
